import { StyleSheet } from "react-native";

import { dimensions } from "../../utilities/common";

export default StyleSheet.create({
  addAddressBtn: { marginVertical: 20 },
  mapLoader: { height: dimensions.windowHeight * 0.5 },
});
